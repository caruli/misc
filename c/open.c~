#include <stdio.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>

#include <unistd.h>

int main() {

  int fd;
  fd = open("myfile.txt", O_CREAT | O_WRONLY, 0600 );

  printf("%d\n",fd);

  
  close(fd);































  return 0;
}
